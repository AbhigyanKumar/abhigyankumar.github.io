const autoType = new Typed(".auto-type", {
	strings: ["developer", "researcher", "optimist", "internet guy", "learner"],
	typeSpeed: 100,
	backDelay: 1000,
	smartBackspace: true,
	backSpeed: 300,
	showCursor: true,
	fadeOut: false,
	loop: true
})
